---
phase: 04-chunking
plan: 02
type: execute
wave: 2
depends_on: ["04-chunking/04-01"]
files_modified:
  - lib/analysis_merger.py
  - tests/unit/test_analysis_merger.py
  - tests/unit/conftest.py
autonomous: true
requirements:
  - CHUNK-04
  - CHUNK-05
security_enforcement: true
tags: [chunking, merger, schema, aggregation]

must_haves:
  truths:
    - "`lib/analysis_merger.py` exports ONE public function: `merge_analyses(chunks: list[tuple[Chunk, dict]], provider: str) -> dict` — returns a canonical `video_analysis` artifact that passes `validate_artifact(\"video_analysis\", merged)`"
    - "Merger handles `len(chunks) == 1` as pass-through: returns `chunks[0][1]` with `chunking_metadata` attached (single-chunk bypass case — covers CHUNK-02 flow through the merger)"
    - "Merger imports `Chunk` from `lib.video_chunker` (Plan 01 contract) and `validate_artifact` from `schemas.artifacts` (Phase 1 gate)"
    - "Private helpers implemented: `_weighted_avg(list[tuple[float, float]]) -> float` (returns 0.0 when total_weight == 0), `_weighted_majority(list[tuple[Any, float]]) -> Any` (returns None on all-None input), `_union_dedup(list[list[str]]) -> list[str]` (insertion-order via `dict.fromkeys`), `_top_n_by_frequency(list[list[str]], n: int) -> list[str]` for color caps (RESEARCH Pitfall 3: n=5), `_worst_confidence(list[str]) -> str` (any 'low' → 'low'; else any 'medium' → 'medium'; else 'high')"
    - "Time-shifted fields (EXHAUSTIVE list per RESEARCH § Time-valued fields): `editing_pacing.energy_arc[].timestamp_s`, `narrative.section_structure[].approx_start_s`, `narrative.section_structure[].approx_end_s` — shifted by `chunk.start_global` on concat; NO OTHER fields require shifting"
    - "Per-field rules match RESEARCH § Merger Rules EXACTLY — every top-level dimension field gets a dispatch: `editing_pacing.total_shots` sum, `cuts_per_minute` weighted avg, `pacing_style` weighted-majority, `shot_type_distribution` per-key weighted-avg re-normalized to sum≈1.0, `transition_types` union-dedup, `energy_arc` concat-with-shift"
    - "`audio`: `has_narration`/`has_music`/`has_sfx` OR-aggregated; `narration_wpm` weighted avg across chunks where `has_narration=true` ONLY; `music_tempo_bpm` weighted avg of non-null values (null if all null); `suggested_tts_voice_profile`/`suggested_music_prompt` = first-chunk (creative-hint consistency per RESEARCH)"
    - "`visual_style`: `color_palette.primary/accent/background/text` + `dominant_colors_hex` capped at 5 via `_top_n_by_frequency` (RESEARCH Pitfall 3); `aspect_ratio` = first chunk (physical property; disagreement logged at WARNING); `typography_style` + `playbook_overrides` = first chunk"
    - "`narrative`: `hook_type` + `hook_duration_seconds` from chunks[0] (video-start position — CHUNK-04 spec); `cta_type` + `cta_duration_seconds` from chunks[-1] (video-end position); `section_count` = sum; `section_structure` = concat with `approx_start_s`/`approx_end_s` shifted by chunk.start_global; `target_duration_seconds` = SUM of chunk durations; `paragraph_pattern` = concat with `[chunk N/M]` markers (RESEARCH § Pattern 5)"
    - "Per-dimension `confidence` maps aggregated field-by-field via `_worst_confidence`: ANY chunk 'low' on a field → merged 'low'; else ANY 'medium' → 'medium'; else 'high' (RESEARCH Pitfall 6 — NOT majority vote, NOT average)"
    - "`source`: `type`/`url`/`local_path`/`title`/`resolution` = first chunk; `source.duration_seconds` = SUM of chunk durations (matches `chunking_metadata.total_duration_s`)"
    - "`version` = '2.0' pass-through; `shot_boundary_source` = 'hybrid' if chunks disagree OR any chunk is 'hybrid', else first chunk's value (RESEARCH Open Question 2)"
    - "`chunking_metadata` attached to top-level: `{chunk_count: int, total_duration_s: float, provider: str, per_chunk: [{index, start_s, end_s, cost_usd, provider}, ...]}` — matches schema at `video_analysis.schema.json:312-331`"
    - "`per_chunk[i].cost_usd` is pulled from `chunks[i][1].get('_cost_usd', 0.0)` — merger does NOT call cost_tracker itself; the chunked_analyzer (Plan 03) stamps `_cost_usd` on each per-chunk artifact dict before passing to the merger"
    - "Validation gate: `validate_artifact(\"video_analysis\", merged)` called exactly ONCE at end of `merge_analyses`; raises `jsonschema.ValidationError` on failure (per RESEARCH Pitfall 2: validate at merge-end only, NOT per-chunk in merger)"
    - "Module logger at `logging.getLogger(__name__)`; `aspect_ratio` / physical-property disagreements logged at WARNING with the conflicting values"
    - "No BaseTool ancestry, no registry visibility; no calls to `lib.checkpoint.write_checkpoint`"
  artifacts:
    - path: "lib/analysis_merger.py"
      provides: "Per-dimension merge rules turning list[(Chunk, artifact)] → canonical video_analysis"
      contains: "def merge_analyses"
      min_lines: 280
    - path: "tests/unit/test_analysis_merger.py"
      provides: "Unit tests for every merger rule enumerated in RESEARCH § Merger Rules"
      contains: "def test_"
      min_lines: 350
    - path: "tests/unit/conftest.py"
      provides: "Adds `fake_video_chunks` fixture generating N distinct per-chunk artifacts"
      contains: "fake_video_chunks"
  key_links:
    - from: "lib/analysis_merger.py"
      to: "lib/video_chunker.py"
      via: "from lib.video_chunker import Chunk"
      pattern: "from lib.video_chunker import.*Chunk"
    - from: "lib/analysis_merger.py"
      to: "schemas/artifacts/__init__.py"
      via: "validate_artifact('video_analysis', merged)"
      pattern: "validate_artifact\\(['\"]video_analysis['\"]"
    - from: "tests/unit/test_analysis_merger.py"
      to: "lib/analysis_merger.py"
      via: "from lib.analysis_merger import merge_analyses"
      pattern: "from lib.analysis_merger import"
    - from: "tests/unit/conftest.py"
      to: "lib.video_chunker.Chunk + minimal_video_analysis"
      via: "fake_video_chunks fixture composes Chunks with schema-valid per-chunk artifacts"
      pattern: "def fake_video_chunks"
---

<objective>
Ship `lib/analysis_merger.py` — the pure-lib per-dimension aggregator that walks a list of `(Chunk, per_chunk_artifact)` pairs and returns a single schema-valid canonical `video_analysis` artifact with globally-shifted timecodes, duration-weighted averages/votes, first-chunk `hook_*`, last-chunk `cta_*`, worst-case confidence, and full `chunking_metadata`.

Purpose: CHUNK-04 + CHUNK-05. Wave-2 work. Depends on Plan 01's `Chunk` namedtuple and on the canonical schema already locked in Phase 1. Consumed by Plan 03 (orchestrator). The merger is the schema-validation gate for chunked analysis — a bug here corrupts every downstream pipeline synthesis.

Output: `lib/analysis_merger.py` (≥280 lines), `tests/unit/test_analysis_merger.py` (≥350 lines covering every rule in RESEARCH § Merger Rules), and a new `fake_video_chunks` fixture in `tests/unit/conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chunking/04-CONTEXT.md
@.planning/phases/04-chunking/04-RESEARCH.md
@.planning/phases/04-chunking/04-01-PLAN.md
@schemas/artifacts/video_analysis.schema.json
@schemas/artifacts/__init__.py
@tests/unit/conftest.py
@tests/contracts/test_video_analysis_schema.py

<interfaces>
<!-- Extracted from existing codebase + Plan 01 contract. Do NOT re-explore -->

From lib/video_chunker.py (Plan 01):
```python
Chunk = namedtuple("Chunk", ["start_global", "end_global", "local_path"])
# start_global and end_global are SECONDS in the ORIGINAL video timeline
```

From schemas/artifacts/__init__.py (Phase 1):
```python
def load_schema(name: str) -> dict: ...
def validate_artifact(name: str, data: dict) -> None:
    """Raises jsonschema.ValidationError on failure."""
```

From tests/contracts/test_video_analysis_schema.py (Phase 1 — single source of truth for fixture):
```python
def minimal_video_analysis() -> dict:
    """Returns a deep-valid minimal artifact covering all 4 dimensions."""
```

From schemas/artifacts/video_analysis.schema.json (already in repo — the contract every rule below derives from):
- Top-level required: version, source, editing_pacing, audio, visual_style, narrative
- editing_pacing required: total_shots, cuts_per_minute, avg_shot_duration_seconds, pacing_style, shot_type_distribution, motion_type_distribution
- audio required: has_narration, has_music, narration_style, voice_music_mix
- visual_style required: color_palette, production_quality, aspect_ratio
- narrative required: hook_type, narrative_arc, target_platform, target_duration_seconds, content_tone
- chunking_metadata: {chunk_count, total_duration_s, provider, per_chunk[{index, start_s, end_s, cost_usd, provider}]}
- shot_boundary_source enum: scene_detect | model | hybrid

From RESEARCH § Merger Rules — the COMPLETE rule table per field (USE DIRECTLY, do not re-derive):
(Full table in RESEARCH.md lines 329-441 — dispatch every required + optional field accordingly)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create fake_video_chunks fixture + scaffold merger with weighted-math primitives + pass-through for N=1</name>
  <files>lib/analysis_merger.py, tests/unit/conftest.py, tests/unit/test_analysis_merger.py</files>
  <read_first>
    - RESEARCH § Pattern 4 (weighted_majority, weighted_avg helpers — copy these verbatim)
    - RESEARCH § Pattern 5 (freetext concat with [chunk N/M] markers)
    - RESEARCH § Merger Rules (reference table during implementation)
    - tests/contracts/test_video_analysis_schema.py (minimal_video_analysis helper)
    - schemas/artifacts/video_analysis.schema.json lines 312-331 (chunking_metadata shape)
  </read_first>
  <behavior>
    Tests in tests/unit/test_analysis_merger.py (schema validation always asserted at end):

    Primitives:
    - test_weighted_avg_basic: [(10.0, 1.0), (20.0, 3.0)] → 17.5
    - test_weighted_avg_zero_weights: [(5.0, 0.0), (10.0, 0.0)] → 0.0 (no div-by-zero)
    - test_weighted_majority_basic: [("a", 2.0), ("b", 5.0), ("a", 1.0)] → "b"
    - test_weighted_majority_ignores_none: [(None, 100.0), ("x", 1.0)] → "x"
    - test_union_dedup_preserves_order: [["a","b"], ["b","c","a"]] → ["a","b","c"]
    - test_top_n_by_frequency: [["#fff","#000"], ["#fff","#111"], ["#000"]] with n=2 → ["#fff","#000"] (most frequent first, tie broken by first-seen)
    - test_worst_confidence_low_wins: ["high","low","medium"] → "low"
    - test_worst_confidence_all_high: ["high","high"] → "high"
    - test_worst_confidence_mixed_no_low: ["high","medium"] → "medium"

    Single-chunk pass-through:
    - test_single_chunk_passthrough: len(chunks)==1 → returns chunks[0][1] deep-copied with `chunking_metadata` added; `chunking_metadata.chunk_count == 1`; `validate_artifact("video_analysis", merged)` passes
    - test_single_chunk_source_duration_unchanged: single-chunk input with `source.duration_seconds=120.0` → merged still 120.0
    - test_chunking_metadata_shape: all fields present, per_chunk has 1 entry with correct index/start/end/cost/provider

    Fixture:
    - test_fake_video_chunks_fixture_validates: generating N=3 chunks via new fixture → each per-chunk artifact passes `validate_artifact("video_analysis", artifact)`
  </behavior>
  <action>
    1. Add `fake_video_chunks` fixture to `tests/unit/conftest.py`:
       ```python
       @pytest.fixture
       def fake_video_chunks():
           """Factory: build list[(Chunk, artifact_dict)] with distinct values per chunk
           so merger tests can assert weighted math.

           Usage: chunks = fake_video_chunks(n=3,
                                             overrides=[{"editing_pacing": {...}}, ...])
           """
           from lib.video_chunker import Chunk
           import copy
           def _build(n: int = 3, overrides: list[dict] | None = None,
                      chunk_seconds: float = 300.0):
               overrides = overrides or [{} for _ in range(n)]
               result = []
               for i in range(n):
                   art = copy.deepcopy(_minimal())
                   # deep-merge override dict into artifact
                   _deep_merge(art, overrides[i] if i < len(overrides) else {})
                   start = i * chunk_seconds
                   end = start + chunk_seconds
                   result.append((Chunk(start_global=start, end_global=end,
                                        local_path=f"/tmp/chunk_{i:03d}.mp4"), art))
               return result
           return _build

       def _deep_merge(dst: dict, src: dict) -> None:
           for k, v in src.items():
               if isinstance(v, dict) and isinstance(dst.get(k), dict):
                   _deep_merge(dst[k], v)
               else:
                   dst[k] = v
       ```

    2. Create `lib/analysis_merger.py` with:
       - Module docstring citing CHUNK-04/CHUNK-05 and RESEARCH § Merger Rules as the rule source
       - Imports: `copy`, `logging`, `collections.Counter`, `typing.Any`
       - `from lib.video_chunker import Chunk`
       - `from schemas.artifacts import validate_artifact`
       - `logger = logging.getLogger(__name__)`
       - Private helpers (implement per <behavior>): `_weighted_avg`, `_weighted_majority`, `_union_dedup`, `_top_n_by_frequency`, `_worst_confidence`, `_concat_freetext`, `_deep_get(d, path)` (helper for nested reads)
       - Public `merge_analyses(chunks, provider)`:
         - If `len(chunks) == 0`: raise `ValueError("merge_analyses requires ≥1 chunk")`
         - If `len(chunks) == 1`: deep-copy chunks[0][1], attach chunking_metadata, validate, return
         - Else: leave `NotImplementedError("Task 2 implements multi-chunk dispatch")` — next task fills in
       - `chunking_metadata` builder extracts `_cost_usd` (default 0.0) from each per-chunk artifact; `total_duration_s = sum(c.end_global - c.start_global for c, _ in chunks)`

    3. Append tests for all primitives + single-chunk pass-through + fixture (per <behavior>).

    4. Run `python -m pytest tests/unit/test_analysis_merger.py -x -q` — all green.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_analysis_merger.py -x -q</automated>
  </verify>
  <done>
    - `fake_video_chunks` fixture available in conftest
    - `lib/analysis_merger.py` has all 7 private primitives + single-chunk path
    - 12 unit tests passing (9 primitives + 3 single-chunk + 1 fixture sanity = 13 actually)
    - Single-chunk merged artifact validates against canonical schema
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-chunk dispatch — editing_pacing + audio + visual_style + narrative + shot_boundary_source + final schema validation</name>
  <files>lib/analysis_merger.py, tests/unit/test_analysis_merger.py</files>
  <read_first>
    - RESEARCH § Merger Rules (the exhaustive field-by-field table — this is the spec)
    - RESEARCH § Time-valued fields (exhaustive list of 3 fields to shift)
    - RESEARCH Pitfall 2 (validate ONCE at end), Pitfall 3 (color cap N=5), Pitfall 6 (worst-case confidence NOT average)
    - RESEARCH Open Question 2 (shot_boundary_source "hybrid" on cross-chunk disagreement)
  </read_first>
  <behavior>
    Appended to tests/unit/test_analysis_merger.py (each test builds chunks via `fake_video_chunks` with targeted overrides, calls `merge_analyses(chunks, provider="gemini")`, asserts merged values, ALWAYS asserts `validate_artifact("video_analysis", merged)` at the end):

    editing_pacing:
    - test_total_shots_sum: chunk shots=[5,7,9] → merged.editing_pacing.total_shots == 21
    - test_cuts_per_minute_weighted_avg: chunks 300s each with cpm=[6,3,9] → weighted avg = 6.0 (equal weights); validate schema ≥ 0
    - test_cuts_per_minute_unequal_weights: chunks [200s, 400s] with cpm=[10,5] → (10*200+5*400)/(200+400) = 6.667
    - test_pacing_style_majority_vote: chunks=[rapid_fire,rapid_fire,dynamic_social] durations [300,300,600] → dynamic_social (weighted)
    - test_shortest_shot_min / test_longest_shot_max: across chunks → min/max correctly
    - test_shot_type_distribution_renormalized: per-key weighted avg → final dict sums to 1.0 ±0.01
    - test_transition_types_union_dedup: chunks=[[cut,dissolve],[cut,fade_black]] → [cut,dissolve,fade_black]
    - test_energy_arc_concat_with_shift: chunk1 has `{timestamp_s: 10}`, chunk2 (start_global=300) has `{timestamp_s: 50}` → merged has `[{timestamp_s:10},{timestamp_s:350}]`

    audio:
    - test_has_narration_or: any chunk True → merged True
    - test_narration_wpm_narration_only_weighted: chunk1 (has_narration=True, wpm=150, 200s), chunk2 (has_narration=False, 400s) → wpm = 150 (only narration chunks count)
    - test_music_tempo_all_null: all chunks music_tempo_bpm=null → merged null
    - test_music_tempo_mixed: [null, 120, 130] → avg of non-null = 125
    - test_voice_music_mix_majority_vote: weighted majority
    - test_suggested_tts_voice_profile_first_chunk: chunk1 value wins regardless of others

    visual_style:
    - test_color_palette_capped_at_5: 8 distinct primary colors across chunks → merged primary has exactly 5
    - test_aspect_ratio_first_chunk_wins: disagreement logged at WARNING (capture via caplog); merged uses chunk[0]
    - test_typography_style_first_chunk: prose description from chunk[0]
    - test_production_quality_majority_vote: weighted majority
    - test_dominant_colors_hex_top_5_by_frequency

    narrative:
    - test_hook_from_first_chunk: chunk[0] hook_type=question, chunk[1]=bold_claim → merged hook_type=question
    - test_cta_from_last_chunk: chunk[-1] cta_type=subscribe, chunk[0]=none_detected → merged cta_type=subscribe
    - test_hook_duration_from_first / test_cta_duration_from_last
    - test_section_count_sum
    - test_section_structure_concat_and_shift: chunk2 (start_global=300) section `{approx_start_s:10, approx_end_s:60}` → merged has `{approx_start_s:310, approx_end_s:360}`
    - test_target_duration_seconds_sum_of_chunks: 3 chunks 300s each → 900.0
    - test_paragraph_pattern_concat_with_markers: merged contains `"[chunk 1/3]"` AND `"[chunk 2/3]"`

    Confidence:
    - test_confidence_worst_case_per_dimension: chunk confidences [{cpm:high},{cpm:low},{cpm:medium}] → merged.editing_pacing.confidence.cpm == "low"
    - test_confidence_all_high_stays_high
    - test_confidence_mixed_no_low: high + medium → medium

    shot_boundary_source:
    - test_shot_boundary_source_all_model: merged = "model"
    - test_shot_boundary_source_any_hybrid_or_disagree: mix of "scene_detect" + "model" → "hybrid" (disagreement rule); mix with one "hybrid" → "hybrid"

    chunking_metadata:
    - test_chunking_metadata_per_chunk_populated: each entry has index/start_s/end_s/cost_usd/provider matching input Chunks and `_cost_usd` hints
    - test_chunking_metadata_total_duration_matches_source: chunking_metadata.total_duration_s == merged.source.duration_seconds

    Validation:
    - test_merged_artifact_schema_valid_N3 / test_merged_artifact_schema_valid_N10
    - test_validate_called_once: spy on validate_artifact → called exactly once per merge (Pitfall 2)
    - test_N3_roundtrip_idempotent: 3 identical chunks → merged values match chunk[0] where rules say "first chunk"
  </behavior>
  <action>
    1. Implement the multi-chunk branch in `merge_analyses`. Structure:

       ```python
       def merge_analyses(chunks, provider):
           if not chunks:
               raise ValueError("merge_analyses requires ≥1 chunk")
           if len(chunks) == 1:
               return _pass_through_single(chunks[0], provider)
           weights = [(c.end_global - c.start_global) for c, _ in chunks]
           merged = {
               "version": "2.0",
               "source": _merge_source(chunks),
               "editing_pacing": _merge_editing_pacing(chunks, weights),
               "audio": _merge_audio(chunks, weights),
               "visual_style": _merge_visual_style(chunks, weights),
               "narrative": _merge_narrative(chunks, weights),
           }
           sbs = _merge_shot_boundary_source([a.get("shot_boundary_source") for _, a in chunks])
           if sbs is not None:
               merged["shot_boundary_source"] = sbs
           merged["chunking_metadata"] = _build_chunking_metadata(chunks, provider)
           validate_artifact("video_analysis", merged)
           return merged
       ```

    2. Implement the 4 per-dimension merge functions PLUS `_merge_source`, `_merge_shot_boundary_source`, `_build_chunking_metadata`. Use the exhaustive field table from RESEARCH § Merger Rules as the spec — dispatch each field to the appropriate helper.

       Critical details:
       - `_merge_editing_pacing`: `shot_type_distribution` must re-normalize after per-key weighted avg (sum to 1.0 ±0.01 — use `_renormalize(d)` helper)
       - `_merge_audio`: `narration_wpm` weighted ONLY across chunks where `has_narration=True`; `music_tempo_bpm` weighted avg of non-null values, returns None if all null
       - `_merge_visual_style`: `color_palette.primary/accent/background/text` + `dominant_colors_hex` → `_top_n_by_frequency(..., n=5)`; `aspect_ratio` disagreement logged at WARNING
       - `_merge_narrative`: `hook_*` from chunks[0]; `cta_*` from chunks[-1]; `section_structure` concat with `approx_start_s += chunk.start_global` AND `approx_end_s += chunk.start_global`; `target_duration_seconds` = sum of chunk durations; `paragraph_pattern` = `_concat_freetext([art["narrative"].get("paragraph_pattern") for _,art in chunks], [f"chunk {i+1}/{len(chunks)}" for i in range(len(chunks))])`
       - `_merge_shot_boundary_source`: if any "hybrid" OR values differ → "hybrid"; else first value; if all None → None (don't add the field)
       - Per-dimension `confidence` maps: `_merge_confidence_map(list_of_confidence_dicts)` → per-key `_worst_confidence`

    3. Implement `_build_chunking_metadata(chunks, provider)`:
       ```python
       per_chunk = []
       for i, (c, art) in enumerate(chunks):
           per_chunk.append({
               "index": i,
               "start_s": c.start_global,
               "end_s": c.end_global,
               "cost_usd": float(art.get("_cost_usd", 0.0)),
               "provider": art.get("_provider_used", provider),
           })
       return {
           "chunk_count": len(chunks),
           "total_duration_s": sum(c.end_global - c.start_global for c, _ in chunks),
           "provider": provider,
           "per_chunk": per_chunk,
       }
       ```

    4. `_merge_source`: take chunks[0][1]["source"] as base, override `duration_seconds` with the sum-of-chunk-durations.

    5. Append all <behavior> tests. Use `caplog` for the aspect_ratio WARNING test. Use `unittest.mock.patch` on `lib.analysis_merger.validate_artifact` for the "called once" test.

    6. Run full merger test file: `python -m pytest tests/unit/test_analysis_merger.py -x -q` — all ≥35 tests green.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_analysis_merger.py -x -q</automated>
  </verify>
  <done>
    - `merge_analyses` dispatches every required + optional field per RESEARCH § Merger Rules
    - All time-shifted fields (energy_arc, section_structure.approx_start_s, approx_end_s) shifted correctly
    - Confidence is worst-case min (any low → low); NOT averaged
    - Colors capped at 5 by frequency
    - hook_* from chunks[0], cta_* from chunks[-1]
    - `shot_boundary_source` = "hybrid" on disagreement
    - `chunking_metadata` fully populated with per_chunk cost stamps
    - `validate_artifact("video_analysis", merged)` called exactly once per merge
    - All ≥35 unit tests green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider → merger input | Per-chunk artifact dicts arrive from provider tools (already validated per-chunk upstream); merger treats them as trusted-shape but untrusted-content |
| merger → validate_artifact | Final schema gate — any merger bug surfaces here as ValidationError (fail loud) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering / Silent Corruption | Merger producing a schema-invalid artifact that downstream (Phase 5 synthesizer) consumes | mitigate | Mandatory `validate_artifact("video_analysis", merged)` at end of merge_analyses — raises jsonschema.ValidationError; test_merged_artifact_schema_valid_* enforces |
| T-04-08 | Information Disclosure (Integrity) | Confidence "low" signal diluted by majority vote → downstream consumes bad data as "high" | mitigate | `_worst_confidence` uses worst-case min (any low → low); Pitfall 6 cited; test_confidence_worst_case_per_dimension guards |
| T-04-09 | Denial of Service | Unbounded array growth (transition_types, dominant_colors_hex) blowing up synthesizer prompt size | mitigate | `_top_n_by_frequency(..., n=5)` caps color-like arrays; union-dedup on enum-space arrays (transitions) — bounded to enum cardinality naturally |
| T-04-10 | Repudiation | per_chunk cost traceability | mitigate | `chunking_metadata.per_chunk[].cost_usd` + `.provider` stamps make per-run cost inspection deterministic |
| T-04-11 | Tampering | Time-value fields not shifted → downstream treats chunk-local timestamps as global | mitigate | Exhaustive time-shifted field list in must_haves; dedicated tests test_energy_arc_concat_with_shift and test_section_structure_concat_and_shift |
</threat_model>

<verification>
**Phase-level checks after plan complete:**
- `python -m pytest tests/unit/test_analysis_merger.py -x -q` → all green (≥35 tests)
- `grep -c 'validate_artifact' lib/analysis_merger.py` → exactly 2 matches (1 import, 1 call at merge end)
- `python -c "from lib.analysis_merger import merge_analyses; from lib.video_chunker import Chunk; print('OK')"` → OK (imports work)
- No side-effects: `grep -n 'write_checkpoint\|cost_tracker' lib/analysis_merger.py` → zero matches (merger must not call either)
</verification>

<success_criteria>
- [ ] `lib/analysis_merger.py` ≥280 lines with all 4 per-dimension merge functions
- [ ] Every required field from RESEARCH § Merger Rules has a dispatch
- [ ] All 3 time-shifted fields shift by `chunk.start_global`
- [ ] Confidence aggregation is worst-case min, NEVER average/majority
- [ ] Color-like arrays capped at N=5 by frequency
- [ ] Schema validation fires exactly once at merge-end
- [ ] N=1 case passes through (for single-chunk bypass from Plan 01)
- [ ] `chunking_metadata` shape matches schema lines 312-331
- [ ] Test file has ≥35 tests covering every rule + confidence + chunking_metadata
</success_criteria>

<output>
After completion, create `.planning/phases/04-chunking/04-02-SUMMARY.md` documenting:
- Line counts (merger + tests)
- Test count and coverage per rule category (editing_pacing, audio, visual_style, narrative, confidence, chunking_metadata)
- Any RESEARCH Assumption (A7 color cap, A8 chunk markers) validated or flagged
- Answer to Open Question 2 (shot_boundary_source) as implemented
- Pointer to Plan 03 (consumes `merge_analyses`)
</output>
