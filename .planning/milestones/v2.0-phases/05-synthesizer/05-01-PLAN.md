---
phase: 05-synthesizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - schemas/pipelines/pipeline_manifest.schema.json
  - lib/pipeline_synthesizer.py
  - pipeline_defs/animated-explainer.yaml
  - pipeline_defs/animation.yaml
  - pipeline_defs/avatar-spokesperson.yaml
  - pipeline_defs/cinematic.yaml
  - pipeline_defs/clip-factory.yaml
  - pipeline_defs/documentary-montage.yaml
  - pipeline_defs/framework-smoke.yaml
  - pipeline_defs/hybrid.yaml
  - pipeline_defs/localization-dub.yaml
  - pipeline_defs/podcast-repurpose.yaml
  - pipeline_defs/screen-demo.yaml
  - pipeline_defs/talking-head.yaml
  - tests/unit/test_pipeline_synthesizer.py
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
  - SYNTH-04
  - SYNTH-05
  - SYNTH-06
tags:
  - synthesis
  - yaml
  - rule-matching
must_haves:
  truths:
    - "ruamel.yaml is installed and importable in the dev container"
    - "pipeline_defs/_staging/ is gitignored"
    - "pipeline_manifest schema accepts an optional expected_analysis top-level block"
    - "All 12 existing pipeline_defs/*.yaml carry an expected_analysis block and still load via lib/pipeline_loader.load_pipeline()"
    - "lib/pipeline_synthesizer.synthesize_pipeline(analysis) writes a YAML file ONLY under pipeline_defs/_staging/<slug>.yaml"
    - "The same video_analysis artifact produces the same slug on re-run (content-hash idempotency)"
    - "Rule-based matcher returns {base_pipeline, match_score, alternatives} with scores bounded in [0, 1]"
    - "Staging YAML carries a provenance header comment (# synthesized from video_analysis checksum: ...)"
  artifacts:
    - path: "lib/pipeline_synthesizer.py"
      provides: "match_base_pipeline + synthesize_pipeline + _canonical_sha256 + _build_slug + _write_staging (module-level, not a class)"
      min_lines: 200
    - path: "requirements.txt"
      provides: "ruamel.yaml>=0.18,<0.20 pinned"
      contains: "ruamel.yaml"
    - path: "schemas/pipelines/pipeline_manifest.schema.json"
      provides: "optional expected_analysis top-level object"
      contains: "expected_analysis"
    - path: "pipeline_defs/cinematic.yaml"
      provides: "expected_analysis annotation (sample canonical case)"
      contains: "expected_analysis:"
    - path: "tests/unit/test_pipeline_synthesizer.py"
      provides: "matcher + slug + writer + module-shape unit tests"
      min_lines: 120
  key_links:
    - from: "lib/pipeline_synthesizer.py"
      to: "lib/pipeline_loader.list_pipelines()"
      via: "import — iterate pipelines for matcher"
      pattern: "from lib\\.pipeline_loader import"
    - from: "lib/pipeline_synthesizer.py"
      to: "ruamel.yaml.YAML"
      via: "ruamel YAML(typ='rt') dumper in _write_staging"
      pattern: "from ruamel\\.yaml import YAML"
    - from: "pipeline_defs/*.yaml"
      to: "schemas/pipelines/pipeline_manifest.schema.json"
      via: "jsonschema.validate inside load_pipeline"
      pattern: "expected_analysis"
---

<objective>
Stand up the core of `lib/pipeline_synthesizer.py`: content-hash checksum + rule-based base-pipeline matcher + ruamel.yaml staging writer. Unblock all downstream plans by installing `ruamel.yaml`, extending the manifest schema to accept `expected_analysis`, and annotating all 12 base pipelines with their matching signals.

Purpose: Deliver a staging-only synthesizer that is deterministic, idempotent, and 100% rule-based (no LLM in this plan). Everything Plan 05-02 (validation/record) and Plan 05-03 (accept/reject + LLM fill) layers on top.

Output: A module that, given a valid `video_analysis` artifact, emits `pipeline_defs/_staging/<slug>.yaml` with a provenance header and returns the match result. Covers SYNTH-01, SYNTH-02, SYNTH-04 (mode parameter accepted + routed, no LLM-fill yet), SYNTH-05, SYNTH-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-synthesizer/05-CONTEXT.md
@.planning/phases/05-synthesizer/05-RESEARCH.md

@lib/pipeline_loader.py
@lib/playbook_generator.py
@schemas/pipelines/pipeline_manifest.schema.json
@schemas/artifacts/video_analysis.schema.json
@pipeline_defs/cinematic.yaml

<interfaces>
<!-- Key types / contracts executor needs. Extracted from codebase — no exploration required. -->

From lib/pipeline_loader.py:
```python
PIPELINE_DEFS_DIR: Path  # <repo>/pipeline_defs
SCHEMA_PATH: Path         # <repo>/schemas/pipelines/pipeline_manifest.schema.json
def load_pipeline(name: str, defs_dir: Optional[Path] = None) -> dict[str, Any]  # validates
def list_pipelines(defs_dir: Optional[Path] = None) -> list[str]  # non-recursive glob("*.yaml")
```

From schemas/artifacts/video_analysis.schema.json (fields the matcher reads):
```
analysis["editing_pacing"]["pacing_style"]
  ∈ {"slow_contemplative","steady_educational","dynamic_social","rapid_fire","variable"}
analysis["editing_pacing"]["shot_type_distribution"]
  keys: talking_head | b_roll | text_card | animation  (floats 0..1)
analysis["editing_pacing"]["motion_type_distribution"]
  keys: motion_clip | animated_still | static_image  (floats ≥0)
analysis["narrative"]["section_structure"]  → list[ { label, approx_start_s, approx_end_s, summary? } ]
```
NOTE: Canonical schema has NO `close_up` key — research finding #4. The pipeline-side
annotation name `close_up_heavy` maps to `shot_type_distribution.talking_head > 0.5`
inside the matcher. Document this mapping in module docstring.

From schemas/pipelines/pipeline_manifest.schema.json (root has `additionalProperties: false`):
  → MUST extend root `properties` with optional `expected_analysis` object + keep root closed.

Expected `expected_analysis` block shape (additive, optional):
```yaml
expected_analysis:
  pacing_style: slow_contemplative   # enum from video_analysis schema
  close_up_heavy: true                # boolean — matches talking_head > 0.5 signal
  static_heavy: false                 # boolean — matches static_image > 0.5 signal
  expected_stage_count: 8             # int ≥ 1
```

Matcher signal weights (locked from CONTEXT.md):
  pacing exact = +0.40; pacing adjacent = +0.15
  close_up_heavy True AND talking_head > 0.5 = +0.25
  static_heavy  True AND static_image > 0.5  = +0.20
  expected_stage_count == len(section_structure) = +0.15
  Bounded [0, 1]. Ties broken alphabetically.

Pacing adjacency graph (undirected): slow_contemplative ↔ steady_educational ↔
dynamic_social ↔ rapid_fire ↔ variable (chain). Adjacent = |idx difference| == 1.
</interfaces>

<pipelines_annotation_guide>
Per-pipeline `expected_analysis` annotations (one block per YAML, added at root level).
Values chosen to make the matcher return each as top-1 on a canonical signal profile.

| Pipeline               | pacing_style         | close_up_heavy | static_heavy | expected_stage_count |
|------------------------|----------------------|----------------|--------------|----------------------|
| animated-explainer     | steady_educational   | false          | true         | 6                    |
| animation              | steady_educational   | false          | true         | 5                    |
| avatar-spokesperson    | steady_educational   | true           | false        | 5                    |
| cinematic              | slow_contemplative   | false          | false        | 8                    |
| clip-factory           | rapid_fire           | false          | false        | 4                    |
| documentary-montage    | slow_contemplative   | false          | false        | 7                    |
| framework-smoke        | dynamic_social       | false          | false        | 3                    |
| hybrid                 | variable             | false          | false        | 6                    |
| localization-dub       | steady_educational   | false          | false        | 4                    |
| podcast-repurpose      | dynamic_social       | true           | false        | 5                    |
| screen-demo            | steady_educational   | false          | true         | 6                    |
| talking-head           | steady_educational   | true           | false        | 5                    |

Executor: count stages in each pipeline file when adding the block. If the actual stage
count differs from the table above (the numbers are estimates), use the ACTUAL count —
determinism matters more than the table. Re-verify cinematic = 8 (research + proposal +
script + scene_plan + assets + edit + compose + publish → 8 ✓).
</pipelines_annotation_guide>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 deps + schema extension + _staging gitignore</name>
  <read_first>
    - requirements.txt (confirm ruamel.yaml not present)
    - .gitignore (confirm pipeline_defs/_staging/ not present)
    - schemas/pipelines/pipeline_manifest.schema.json (line 8 properties block, line 160 additionalProperties:false)
  </read_first>
  <files>
    - requirements.txt
    - .gitignore
    - schemas/pipelines/pipeline_manifest.schema.json
  </files>
  <action>
    1. Append `ruamel.yaml>=0.18,<0.20` to `requirements.txt` (new line after `openai>=1.0,<3`). Research Finding #3 + Open Question 5 — do NOT defer to Phase 6.
    2. Run `pip install ruamel.yaml>=0.18,<0.20` inside the devcontainer (verify with `python -c "import ruamel.yaml; print(ruamel.yaml.__version__)"`).
    3. Append `pipeline_defs/_staging/` to `.gitignore` (new line under the "Pipeline outputs (regenerable — never commit large media)" section, next to `pipeline/`).
    4. Extend `schemas/pipelines/pipeline_manifest.schema.json` root `properties` with an OPTIONAL `expected_analysis` object (additive, non-breaking per Pitfall 1). Insert between `metadata` and `orchestration`:
       ```json
       "expected_analysis": {
         "type": "object",
         "description": "Signals used by pipeline_synthesizer (Phase 5) for rule-based matching. Optional and additive; absence = no signal contribution.",
         "properties": {
           "pacing_style": {
             "type": "string",
             "enum": ["slow_contemplative","steady_educational","dynamic_social","rapid_fire","variable"]
           },
           "close_up_heavy":       { "type": "boolean" },
           "static_heavy":         { "type": "boolean" },
           "expected_stage_count": { "type": "integer", "minimum": 1 }
         },
         "additionalProperties": false
       },
       ```
       Root `additionalProperties: false` stays — the new key is now declared and permitted. Do NOT change anything else in the schema.
    5. Re-run existing pipeline contract tests to confirm no regression.
  </action>
  <verify>
    <automated>python -c "import ruamel.yaml; print(ruamel.yaml.__version__)" &amp;&amp; python -c "import json; s=json.load(open('schemas/pipelines/pipeline_manifest.schema.json')); assert 'expected_analysis' in s['properties']; print('schema ok')" &amp;&amp; grep -q '_staging' .gitignore &amp;&amp; pytest tests/contracts -x -q -k "pipeline or manifest" 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `ruamel.yaml` importable; version &gt;= 0.18, &lt; 0.20
    - `.gitignore` contains `pipeline_defs/_staging/`
    - `schemas/pipelines/pipeline_manifest.schema.json` declares `expected_analysis` optional object, root remains `additionalProperties: false`
    - Existing pipeline contract tests still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/pipeline_synthesizer.py core — checksum + matcher + staging writer (SYNTH-01, 02, 04, 05, 06)</name>
  <read_first>
    - lib/playbook_generator.py (pattern template — module-level functions, no class, jsonschema.validate inline)
    - lib/pipeline_loader.py (reuse `load_pipeline`, `list_pipelines`, `PIPELINE_DEFS_DIR`)
    - schemas/artifacts/video_analysis.schema.json (required fields matcher reads)
    - 05-RESEARCH.md lines 210–320 (ruamel writer pattern + matcher with signal table)
  </read_first>
  <files>
    - lib/pipeline_synthesizer.py
    - tests/unit/test_pipeline_synthesizer.py
  </files>
  <behavior>
    Test 1 (test_module_shape): import lib.pipeline_synthesizer; assert module (not class) exports
      `match_base_pipeline`, `synthesize_pipeline`, `_canonical_sha256`, `_build_slug`, `_write_staging`.
      Assert no class defined via `inspect.getmembers(mod, inspect.isclass)` returning [] (except imported).
    Test 2 (test_canonical_sha256_deterministic): same dict (different key order input) → same hex.
    Test 3 (test_matcher_exact_pacing): fixture with `pacing_style="slow_contemplative"` + section count 8
      → base_pipeline == "cinematic", match_score ≥ 0.55 (0.40 + 0.15).
    Test 4 (test_matcher_close_up_heavy): fixture talking_head=0.8, pacing=steady_educational, stages=5
      → base_pipeline ∈ {"talking-head","avatar-spokesperson"}, match_score ≥ 0.80.
    Test 5 (test_matcher_static_heavy): fixture static_image=0.9, pacing=steady_educational, stages=6
      → base_pipeline ∈ {"animated-explainer","screen-demo"}, match_score ≥ 0.75.
    Test 6 (test_deterministic_ties): craft fixture where two pipelines tie → winner alphabetically earlier.
    Test 7 (test_slug_idempotent): same analysis → same slug across two calls; slug format
      `{base_pipeline}-{hash[:8]}`.
    Test 8 (test_staging_only_write): synthesize_pipeline writes to
      `pipeline_defs/_staging/<slug>.yaml` and NOT to `pipeline_defs/<slug>.yaml` directly.
    Test 9 (test_provenance_header): staging file's first line starts with
      `# synthesized from video_analysis checksum: <hash>`.
    Test 10 (test_collision_bytes_equal_noop): call synthesize_pipeline twice with same analysis →
      second call returns same staging path; file content unchanged (byte-identical).
    Test 11 (test_collision_bytes_differ_v2): pre-write a different file at the target slug path →
      synthesizer writes `-v2` suffix (`{base}-{hash[:8]}-v2.yaml`).
    Test 12 (test_mode_parameter_accepted): mode="template" and mode="replica" both run without
      error; mode="other" raises ValueError. (LLM fill path is disabled in this plan — stubbed.)
  </behavior>
  <action>
    Create `lib/pipeline_synthesizer.py` as a pure lib module (mirrors `lib/playbook_generator.py`). Module-level functions only. No class.

    **Imports** (top of file):
    ```python
    from __future__ import annotations
    import hashlib
    import json
    import os
    from datetime import datetime, timezone
    from io import StringIO
    from pathlib import Path
    from typing import Any, Literal

    from ruamel.yaml import YAML

    from lib.pipeline_loader import PIPELINE_DEFS_DIR, load_pipeline, list_pipelines
    ```

    **Module constants:**
    ```python
    STAGING_DIR = PIPELINE_DEFS_DIR / "_staging"

    PACING_CHAIN = [
        "slow_contemplative",
        "steady_educational",
        "dynamic_social",
        "rapid_fire",
        "variable",
    ]

    Mode = Literal["template", "replica"]
    ```

    **Public API (SYNTH-01, 02, 04, 05, 06):**

    1. `_canonical_sha256(artifact: dict) -> str`:
       Compute SHA-256 over `json.dumps(artifact, sort_keys=True, separators=(",", ":"), ensure_ascii=False).encode("utf-8")`. Return hex digest. (Research Finding A2 canonical form.)

    2. `_build_slug(base_pipeline: str, checksum: str) -> str`:
       Return `f"{base_pipeline}-{checksum[:8]}"`.

    3. `_is_adjacent_pacing(a: str, b: str) -> bool`:
       Return True iff both in `PACING_CHAIN` and `abs(idx(a) - idx(b)) == 1`.

    4. `match_base_pipeline(analysis: dict) -> dict[str, Any]` (SYNTH-02):
       Returns `{"base_pipeline": str, "match_score": float, "alternatives": [{"name","score"}, ...]}`.
       Iterates `list_pipelines()` (already excludes `_staging/` per non-recursive glob — defense-in-depth filter comes in Plan 05-02).
       For each pipeline, calls `load_pipeline(name)` to read its `expected_analysis` block (missing → score 0).
       Applies the signal table from `<interfaces>` above. Bounded [0, 1]. Sort by `(-score, name)`; top-1 is winner.
       Raises `ValueError` on missing `analysis["editing_pacing"]["pacing_style"]` (schema guarantees it, but explicit anyway).

    5. `_write_staging(manifest: dict, slug: str, *, checksum: str, mode: str, match_score: float, base: str) -> Path` (SYNTH-05):
       - `STAGING_DIR.mkdir(parents=True, exist_ok=True)`
       - `path = STAGING_DIR / f"{slug}.yaml"`
       - Use ruamel: `yaml = YAML(typ="rt"); yaml.preserve_quotes = True; yaml.indent(mapping=2, sequence=4, offset=2); yaml.width = 120; yaml.default_flow_style = False`
       - Dump manifest to a `StringIO()` buffer.
       - Build header lines:
         ```python
         timestamp = datetime.now(timezone.utc).isoformat(timespec="seconds")
         header = (
             f"# synthesized from video_analysis checksum: {checksum}\n"
             f"# base_pipeline: {base}\n"
             f"# match_score: {match_score:.2f}\n"
             f"# mode: {mode}\n"
             f"# at: {timestamp}\n"
         )
         ```
       - **Collision handling (SYNTH-06 + Pitfall 5):**
         - If `path.exists()` and `path.read_text(encoding="utf-8") == (header without the timestamp line + body)` → treat as identical re-run, return existing path (no write).
         - Actually: since timestamp changes on every call, compare bodies only — strip the `# at: ...` line before diffing. If bodies equal → reuse existing path.
         - If `path.exists()` and bodies differ → iterate suffixes `-v2`, `-v3`, ... until free slot. Use that path.
       - `path.write_text(header + buf.getvalue(), encoding="utf-8")`
       - Return `path`.

    6. `synthesize_pipeline(analysis: dict, *, mode: Mode = "template", provider_used: str = "gemini", use_llm_fill: bool | None = None) -> dict[str, Any]` (SYNTH-01):
       This plan's version returns a RUN-RECORD-SHAPED dict but does NOT call `jsonschema.validate` against the pipeline_synthesis schema — that happens in Plan 05-02. Also does NOT invoke LLM fill — that is Plan 05-03. Docstring must state both.
       - Validate `mode` ∈ {"template", "replica"}; else raise `ValueError("mode must be 'template' or 'replica'")`. **Per user decision D SYNTH-04: mode parameter must be accepted now; the two branches diverge in Plan 05-03 where LLM fill lands.**
       - Compute `checksum = _canonical_sha256(analysis)`.
       - Run matcher → `match = match_base_pipeline(analysis)`.
       - Load base manifest → `base_manifest = load_pipeline(match["base_pipeline"])`.
       - For now `synthesized = base_manifest` verbatim (no LLM yet — use_llm_fill parameter accepted but ignored; Plan 05-03 wires it).
       - `slug = _build_slug(match["base_pipeline"], checksum)`.
       - `staging_path = _write_staging(synthesized, slug, checksum=checksum, mode=mode, match_score=match["match_score"], base=match["base_pipeline"])`.
       - Return dict:
         ```python
         {
             "version": "1.0",
             "base_pipeline": match["base_pipeline"],
             "match_score": match["match_score"],
             "mode": mode,
             "staging_path": str(staging_path.relative_to(PIPELINE_DEFS_DIR.parent)),
             "diff_against_base": "",  # filled in Plan 05-02
             "validation_status": "pending",  # finalized in Plan 05-02 after semantic validation
             "source_analysis_checksum": checksum,
             "provider_used": provider_used,
         }
         ```
       - Module docstring must flag: "Plan 05-02 adds `validate_synthesized_pipeline` + jsonschema.validate of the record; Plan 05-03 adds LLM fill + accept/reject API. No `synthesize_and_accept` function exists, ever (SYNTH-10 hard rule)."

    **Test file:** `tests/unit/test_pipeline_synthesizer.py` implementing Tests 1–12 above. Fixtures:
    - Use a `tmp_path` fixture and monkeypatch `PIPELINE_DEFS_DIR` + `STAGING_DIR` to a tmp copy of `pipeline_defs/` (via `shutil.copytree`) for write isolation.
    - Build a minimal valid `video_analysis` artifact fixture covering required fields (version, source, editing_pacing, audio, visual_style, narrative).
    - Test 1 (module_shape): inspect module for classes — assert there are no locally-defined classes (classes imported from other modules are fine — filter by `__module__`).

    **Non-negotiables:**
    - NO class definitions in `lib/pipeline_synthesizer.py`. Module-level functions only (SYNTH-01).
    - NO function named `synthesize_and_accept` or similar (SYNTH-10 hard rule — reviewer must grep for absence in Plan 05-03).
    - NO call to `openai`, `OpenAI`, or any LLM client in this plan (deferred to Plan 05-03).
    - NO write outside `pipeline_defs/_staging/` (SYNTH-05).
  </action>
  <verify>
    <automated>pytest tests/unit/test_pipeline_synthesizer.py -x --tb=short -q</automated>
  </verify>
  <done>
    - `lib/pipeline_synthesizer.py` exists, all 12 behaviors (Tests 1–12) green
    - `match_base_pipeline` returns deterministic top-1 on fixture inputs
    - Staging writes land ONLY under `pipeline_defs/_staging/`
    - Slug format `{base_pipeline}-{hash[:8]}`; collision handling verified (no-op for byte-equal, `-v2` suffix for differing)
    - Provenance header present as first 5 lines of staging file
    - No LLM calls; no class definitions; no `synthesize_and_accept`
  </done>
</task>

<task type="auto">
  <name>Task 3: Annotate all 12 pipeline_defs/*.yaml with expected_analysis (SYNTH-02 enabler)</name>
  <read_first>
    - pipeline_defs/cinematic.yaml (example — verify stage count = 8)
    - pipeline_defs/animated-explainer.yaml (read stages length — update table if differs)
    - pipeline_defs/animation.yaml
    - pipeline_defs/avatar-spokesperson.yaml
    - pipeline_defs/clip-factory.yaml
    - pipeline_defs/documentary-montage.yaml
    - pipeline_defs/framework-smoke.yaml
    - pipeline_defs/hybrid.yaml
    - pipeline_defs/localization-dub.yaml
    - pipeline_defs/podcast-repurpose.yaml
    - pipeline_defs/screen-demo.yaml
    - pipeline_defs/talking-head.yaml
    - pipelines_annotation_guide in this plan's &lt;context&gt;
  </read_first>
  <files>
    - pipeline_defs/animated-explainer.yaml
    - pipeline_defs/animation.yaml
    - pipeline_defs/avatar-spokesperson.yaml
    - pipeline_defs/cinematic.yaml
    - pipeline_defs/clip-factory.yaml
    - pipeline_defs/documentary-montage.yaml
    - pipeline_defs/framework-smoke.yaml
    - pipeline_defs/hybrid.yaml
    - pipeline_defs/localization-dub.yaml
    - pipeline_defs/podcast-repurpose.yaml
    - pipeline_defs/screen-demo.yaml
    - pipeline_defs/talking-head.yaml
  </files>
  <action>
    For each of the 12 base pipelines, add a top-level `expected_analysis:` block using the values from the table in `<pipelines_annotation_guide>` above. Placement: BEFORE `stages:` and AFTER `compatible_playbooks:` (or wherever natural — block ordering isn't semantic).

    **Per-pipeline execution rule:**
    1. Open the YAML. Count the ACTUAL number of top-level `stages:` entries (use `grep -c "^  - name:" pipeline_defs/{name}.yaml`).
    2. If actual count differs from the table, use the ACTUAL count. Record deviations in the task's done criteria log.
    3. Add the block verbatim (pacing_style, close_up_heavy, static_heavy values from table). Example for cinematic.yaml (8 stages verified):
       ```yaml
       expected_analysis:
         pacing_style: slow_contemplative
         close_up_heavy: false
         static_heavy: false
         expected_stage_count: 8
       ```
    4. Save. Immediately run `python -c "from lib.pipeline_loader import load_pipeline; load_pipeline('<name>')"` — MUST succeed (schema validation). If it fails, the schema extension in Task 1 is wrong; STOP and fix before continuing to other pipelines (Pitfall 1 mitigation).

    After all 12 annotated, run full regression:
    ```
    pytest tests/contracts -x -q -k "pipeline or manifest"
    pytest tests/unit/test_pipeline_synthesizer.py::test_matcher_exact_pacing -x -q
    pytest tests/unit/test_pipeline_synthesizer.py::test_deterministic_ties -x -q
    ```

    **Safety:** If ANY pipeline fails to load after annotation, revert just that pipeline, inspect the schema extension output, and fix before continuing. Do not batch-commit a broken state.
  </action>
  <verify>
    <automated>python -c "from lib.pipeline_loader import list_pipelines, load_pipeline; names=list_pipelines(); assert len(names)==12, f'expected 12 base pipelines, got {len(names)}: {names}'; [load_pipeline(n) for n in names]; print('all 12 ok')" &amp;&amp; pytest tests/contracts -x -q -k "pipeline or manifest" 2>&amp;1 | tail -5 &amp;&amp; pytest tests/unit/test_pipeline_synthesizer.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - All 12 `pipeline_defs/*.yaml` carry an `expected_analysis` block
    - Every pipeline loads via `load_pipeline()` without schema validation error
    - Contract regression green
    - Matcher-related tests from Task 2 still green with real pipeline set (not just fixtures)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → synthesizer | `video_analysis` dict is untrusted input — may fail schema validation |
| synthesizer → filesystem (`pipeline_defs/_staging/`) | Write path must be constrained to staging dir |
| synthesizer → filesystem (`pipeline_defs/` direct) | Never written in this plan (enforced by design — only `_staging/`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `_write_staging` path construction | mitigate | Derive path only as `STAGING_DIR / f"{slug}.yaml"`; slug is sanitized via `_build_slug` (base_pipeline name from closed registry + hex-only hash). Never concatenates external string into path. |
| T-05-02 | Tampering | Input analysis dict causes path-escape via base_pipeline | mitigate | `match_base_pipeline` selects base from `list_pipelines()` output only — closed set of local filenames. Even if analysis is adversarial, base_pipeline cannot contain `/` or `..`. |
| T-05-03 | Information Disclosure | Provenance header leaks paths / PII | accept | Header contains checksum, base name, score, mode, timestamp. No path, no analysis content. Low risk. |
| T-05-04 | Denial of Service | Slug collision loop runs forever on adversarial filesystem | mitigate | Cap `-vN` suffix at N=99; if reached, raise `RuntimeError`. |
| T-05-05 | Repudiation | No audit of who synthesized what | accept | Solo-dev project + human-in-the-loop accept/reject gate in 05-03; staging file with timestamp header suffices. |
| T-05-06 | Elevation of Privilege | Synthesizer promotes to `pipeline_defs/` without approval | mitigate | This plan NEVER writes outside `_staging/`. `accept_synthesis` is the only promotion path, introduced in Plan 05-03 as a separate public function. |
| T-05-07 | Spoofing | Attacker supplies `analysis` dict with crafted fields to force choice of specific pipeline | accept | Base pipeline selection is visible via `match_score` surfaced in return dict; human reviews the staged YAML before `accept_synthesis`. Forcing a choice only affects matched base, not the final accept decision. |
</threat_model>

<verification>
1. `pip show ruamel.yaml` reports a version in `[0.18, 0.20)`.
2. `.gitignore` contains the exact line `pipeline_defs/_staging/`.
3. `jq '.properties | has("expected_analysis")' schemas/pipelines/pipeline_manifest.schema.json` returns `true`.
4. `python -c "from lib.pipeline_loader import list_pipelines; assert len(list_pipelines()) == 12"` succeeds.
5. `python -c "from lib.pipeline_loader import load_pipeline; [load_pipeline(n) for n in ['animated-explainer','animation','avatar-spokesperson','cinematic','clip-factory','documentary-montage','framework-smoke','hybrid','localization-dub','podcast-repurpose','screen-demo','talking-head']]"` — all load.
6. `pytest tests/unit/test_pipeline_synthesizer.py -x -q` — all 12 behaviors green.
7. `pytest tests/contracts -x -q -k "pipeline or manifest"` — backward-compat gate green.
8. `grep -rE "def synthesize_and_accept|def synth_and_accept" lib/` returns nothing.
9. `grep -E "class " lib/pipeline_synthesizer.py` returns nothing (module-level functions only).
10. `ls pipeline_defs/_staging/` returns empty (or only test-produced files cleaned between tests).
</verification>

<success_criteria>
- SYNTH-01: `lib/pipeline_synthesizer.py` is a pure lib module with module-level functions; no class; no orchestration; no LLM calls (LLM lands in Plan 05-03)
- SYNTH-02: `match_base_pipeline` implements the 4-signal rule table; bounded [0, 1]; deterministic ties
- SYNTH-04: `mode` parameter (`"template"`/`"replica"`) accepted and routed; invalid mode raises `ValueError`; LLM branches deferred to Plan 05-03
- SYNTH-05: Writes ONLY to `pipeline_defs/_staging/<slug>.yaml`; provenance header preserved via direct file write, body dumped via ruamel round-trip
- SYNTH-06: Slug `{base_pipeline}-{hash[:8]}` derived from SHA-256 of canonical JSON; idempotent (byte-equal re-write = no-op); collision = `-vN` suffix
- Wave 0 deps installed, schema extended, 12 pipelines annotated
- All tests pass; no backward-compat regression
</success_criteria>

<output>
After completion, create `.planning/phases/05-synthesizer/05-01-SUMMARY.md` covering:
- Files created/modified (list)
- Test count + pass rate
- Deviations from the `<pipelines_annotation_guide>` table (if any pipeline's actual stage count differed)
- Any ruamel.yaml quirks hit (e.g., empty `tools_available: []` rendering — Pitfall 3)
- Hand-off notes for Plan 05-02 (validation + record writer)
</output>
