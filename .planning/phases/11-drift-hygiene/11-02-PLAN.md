---
phase: 11-drift-hygiene
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  # Not pre-enumerable — this plan is a reading pass + classified fix pass over
  # 30 LO/NI items spanning Phase 3, 4, 5 code. Exact files resolved by the
  # reading pass in Task 1 (see classification table it produces). The high-
  # probability set (derived from the REVIEW.md catalogue) is:
  - tools/analysis/openrouter_video_analyzer.py        # Phase 3 LO-01..LO-04, NI-01..NI-05
  - tests/unit/test_openrouter_video_analyzer.py       # Phase 3 NI-04 + any LO backing test
  - lib/analysis_merger.py                             # Phase 4 LO-01..LO-04, NI-03
  - lib/chunked_analyzer.py                            # Phase 4 LO-05, NI-02, NI-04, NI-05
  - lib/video_chunker.py                               # Phase 4 LO-06, NI-01, NI-02
  - lib/pipeline_synthesizer.py                        # Phase 5 LR-01..LR-05, NR-03, NR-04
  - lib/llm_fill.py                                    # Phase 5 LR-03, LR-04, NR-01
  - tests/contracts/test_phase5_synthesis.py           # Phase 5 LR-06
  - tests/unit/test_pipeline_synthesizer.py            # Phase 5 NR-02
  - .planning/REQUIREMENTS.md                          # appended Future Requirements for deferred items
  - .planning/phases/11-drift-hygiene/11-02-SUMMARY.md # classification + dispositions
autonomous: true
requirements: [NITS-01]

must_haves:
  truths:
    - "Every LO and NIT item across the 3 REVIEW.md files has a disposition (fixed | already-resolved | deferred) recorded in 11-02-SUMMARY.md"
    - "Each item marked 'fixed' has a referenced commit SHA"
    - "Each item marked 'already-resolved' has evidence (grep output or prior commit SHA proving the condition no longer holds)"
    - "Each item marked 'deferred' is added to .planning/REQUIREMENTS.md Future Requirements with rationale"
    - "git log --oneline shows ONE commit per fix (not a monolithic absorb commit) — commit count in the fix window >= number of 'fixed' items"
    - "Full suite pytest tests/ --ignore=tests/qa -q stays >=687 passing after every commit (run between commits or at least at plan end)"
    - "Zero new failing tests introduced; zero behavior change beyond the narrow hygiene intent of each finding"
  artifacts:
    - path: ".planning/phases/11-drift-hygiene/11-02-SUMMARY.md"
      provides: "Enumerated classification of all 30 LO/NI items with disposition, commit SHA or rationale, grep/diff evidence"
      contains: "LO-01|LO-02|LO-03|LO-04|LR-01|LR-02|LR-03|LR-04|LR-05|LR-06|NI-01|NI-02|NI-03|NI-04|NI-05|NR-01|NR-02|NR-03|NR-04"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Future Requirements section updated with any items deferred from this plan"
      contains: "Future Requirements"
  key_links:
    - from: "11-02-SUMMARY.md classification table"
      to: "individual fix commits"
      via: "each 'fixed' row lists its commit SHA, produced by git rev-parse HEAD after each atomic commit"
      pattern: "[a-f0-9]{7,40}"
    - from: "11-02-SUMMARY.md deferred items"
      to: ".planning/REQUIREMENTS.md Future Requirements"
      via: "each 'deferred' row has a matching line in REQUIREMENTS.md with the same ID (e.g. NITS-DEF-<origin>-<NN>)"
      pattern: "NITS-DEF"
---

<objective>
Close NITS-01 by absorbing the 16 LOW + 14 NIT findings deferred from v2.0 Phase 3, 4, 5 code reviews. This is a **two-pass plan** — the catalogue is long (30 items), evolving (some may be transparently fixed by Phase 8-10), and requires per-item judgment. A pre-enumerated task list would be fragile and verbose.

Purpose:
- **Pass 1 (READING)**: Read all 3 REVIEW.md files in full, build a classified checklist: each item gets one of `fix` | `already-resolved` | `defer`. This is the gate that catches items Phase 8-10 happened to resolve, and items that would require a real refactor (which must be deferred explicitly per 11-CONTEXT.md D-02).
- **Pass 2 (FIX)**: Execute the classification. Each `fix` is a narrow atomic commit (`fix(11-nits): <ID> <short description>`). Each `already-resolved` is documented in SUMMARY.md with grep evidence. Each `defer` gets added to REQUIREMENTS.md Future Requirements.

Output:
- `11-02-SUMMARY.md` with a row per item (30 rows) showing disposition + commit SHA / evidence
- N atomic commits where N = count of `fix` items (no monolithic squash)
- `.planning/REQUIREMENTS.md` Future Requirements updated for `defer` items
- Full test suite stays >=687 passing throughout
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-drift-hygiene/11-CONTEXT.md
@.planning/milestones/v2.0-phases/03-openrouter-provider/03-REVIEW.md
@.planning/milestones/v2.0-phases/04-chunking/04-REVIEW.md
@.planning/milestones/v2.0-phases/05-synthesizer/05-REVIEW.md
@tools/analysis/openrouter_video_analyzer.py
@lib/analysis_merger.py
@lib/chunked_analyzer.py
@lib/video_chunker.py
@lib/pipeline_synthesizer.py
@lib/llm_fill.py

<interfaces>
<!-- Finding ID catalogue: 30 items across 3 phases. Full details in the linked REVIEW.md files. -->
<!-- Phase 3 (tools/analysis/openrouter_video_analyzer.py): 4 LO + 5 NIT = 9 items -->
<!-- Phase 4 (lib/video_chunker, lib/analysis_merger, lib/chunked_analyzer): 6 LO + 5 NIT = 11 items -->
<!-- Phase 5 (lib/pipeline_synthesizer, lib/llm_fill): 6 LR + 4 NR = 10 items -->
<!-- Executor MUST read the three REVIEW.md files and build the full per-item table during Task 1 -->

Finding ID scheme (as used in REVIEW.md):
  Phase 3: LO-01..LO-04, NI-01..NI-05  (9 items)
  Phase 4: LO-01..LO-06, NI-01..NI-05  (11 items — yes, Phase 4 re-uses the LO-NN numbering, disambiguate by phase)
  Phase 5: LR-01..LR-06, NR-01..NR-04  (10 items — Phase 5 uses LR/NR prefixes already)

Disambiguation for commit messages and SUMMARY table: prefix each ID with its phase.
  Phase 3 items: `P3-LO-01`, `P3-NI-01`, etc.
  Phase 4 items: `P4-LO-01`, `P4-NI-01`, etc.
  Phase 5 items: `P5-LR-01`, `P5-NR-01`, etc.  (keeps native prefix, adds phase)

Commit message convention (per 11-CONTEXT.md D-02):
  `fix(11-nits): <P3-LO-01|P4-LO-01|P5-LR-01|...> <short description>`
  Example: `fix(11-nits): P3-LO-01 remove dead FALLBACK_MODEL constant`
  Example: `refactor(11-nits): P4-NI-03 drop unused Callable import`

Baseline to preserve:
  - Current suite: `pytest tests/ --ignore=tests/qa -q` -> 703 collected, 687+ passing (per STATE.md) with 3 pre-existing fc-list failures and 13 skipped
  - Must stay >=687 passing after every commit
  - 3 fc-list failures are pre-existing and untouchable — do NOT claim they changed
</interfaces>

<high_probability_items>
<!-- Pre-classified hints based on reading the REVIEW.md files. The executor MUST re-verify each during Task 1 — Phase 8-10 may have resolved some of these transparently. These are starting priors, NOT the authoritative list. -->

Likely fix (low-risk, narrow, test-preserving):
  - P3-LO-01 delete dead FALLBACK_MODEL constant + the test import that references it
  - P3-LO-02 add one-line docstring/comment explaining asymmetric ladder at openrouter_video_analyzer.py:455
  - P3-NI-04 correct over-provision comment in test_retry_exhausted_raises (or trim to 4 responses)
  - P3-NI-05 extract "30 words" magic number to module constant COMPACT_FIELD_WORD_LIMIT
  - P4-LO-01 docstring note on _weighted_avg callers re: 0.0 vs None semantics
  - P4-LO-02 field-level comment on _merge_shot_boundary_source "hybrid" escalation
  - P4-LO-03 log a debug/warning when _cost_usd missing + docstring note
  - P4-LO-04 inline comment explaining tempo_seen semantics
  - P4-LO-06 add `if duration <= 0.0: raise VideoChunkingError(...)` guard
  - P4-NI-02 extract _MAX_CHUNK_SECONDS_DEFAULT = 300.0 module constant + replace 4 call sites
  - P4-NI-03 drop unused `Callable` from typing import in analysis_merger.py:57
  - P4-NI-04 remove misleading `# noqa: F401` in chunked_analyzer.py:80
  - P4-NI-05 remove unreferenced _PRICING_VERIFIED_AT OR add sanity-check assertion
  - P5-LR-01 wrap json.dumps(artifact, ...) in try/except TypeError with friendlier message
  - P5-LR-02 log warning in match_base_pipeline when a manifest fails to load (keep 0.0 score)
  - P5-LR-03 remove double env check for VIDEO_SYNTH_LLM_FILL (keep inner, simplify outer)
  - P5-LR-04 delete unreachable `return None` in llm_fill._call_with_retry (or add # pragma)
  - P5-LR-05 scope _body_without_timestamp strip to ISO-8601 regex pattern
  - P5-LR-06 tighten contract-test assertion to exclude "pending" from the accepted validation_status set
  - P5-NR-02 refresh module docstring test count in test_pipeline_synthesizer.py
  - P5-NR-03 reorder _relative_staging_path call in accept_synthesis to before shutil.move
  - P5-NR-04 optional round-trip validation — CHECK if this counts as behavior change; if yes, DEFER

Likely already-resolved (verify with grep — Phase 8-10 may have swept these):
  - P3-NI-03 FALLBACK_MODEL + OPENROUTER_BASE_URL export audit (Phase 8 CLEAN-04 already centralized OPENROUTER_BASE_URL)
  - P3-LO-03 _safe_exc_str key redaction — check if Phase 8 added this in passing
  - P4-LO-05 fail_fast orphan reservations — check if Phase 9 fixed this as a side effect of CLEAN-05/07

Likely defer (would require behavior/contract change):
  - P3-LO-04 markdown fence stripping in prompt-embedded fallback — needs a new test + a model-behavior verification; if borderline, defer with rationale
  - P5-NR-01 `FILLABLE_FIELDS` truthy-check limitation — explicitly a one-way door design choice; document-only = fix, contract change = defer
  - P5-NR-04 round-trip validation after _write_staging — one extra disk-read + schema-validate per synthesis; behavior change, defer

These priors are guidance; final disposition is the executor's call in Task 1 based on current code state.
</high_probability_items>
</context>

<tasks>

<task type="auto">
  <name>Task 1: READING PASS — classify every LO/NI item across Phase 3, 4, 5 reviews</name>
  <files>.planning/phases/11-drift-hygiene/11-02-SUMMARY.md</files>
  <read_first>
    - `.planning/milestones/v2.0-phases/03-openrouter-provider/03-REVIEW.md` (ALL LO + NIT sections, lines 149-251)
    - `.planning/milestones/v2.0-phases/04-chunking/04-REVIEW.md` (ALL LO + NIT sections, lines 95-218)
    - `.planning/milestones/v2.0-phases/05-synthesizer/05-REVIEW.md` (ALL LO + NIT sections, lines 89-211)
    - `.planning/phases/11-drift-hygiene/11-CONTEXT.md` (D-02 disposition rules, deferred-escape-hatch policy)
    - Target source files as listed in each finding's **File:** line — open each to verify the issue still exists in current HEAD
    - Git log spanning Phase 8-10: `git log --oneline v2.0..HEAD -- tools/analysis/openrouter_video_analyzer.py lib/analysis_merger.py lib/chunked_analyzer.py lib/video_chunker.py lib/pipeline_synthesizer.py lib/llm_fill.py` — some items may have been swept already
  </read_first>
  <action>
    Build the classified checklist. Create `.planning/phases/11-drift-hygiene/11-02-SUMMARY.md` as a WORK-IN-PROGRESS classification document. Final SUMMARY emitted at plan end will refine this with commit SHAs.

    **Protocol (strict — do this in order):**

    1. Enumerate all 30 items with canonical IDs (`P3-LO-01..P3-NI-05`, `P4-LO-01..P4-NI-05`, `P5-LR-01..P5-NR-04`). Confirm the count matches what's in the three REVIEW.md files — CONTEXT.md says "the 30 finding count is approximate". Record exact count in SUMMARY.md.

    2. For EACH item, open the file+line referenced in the finding. Decide disposition:

       **`fix`** — issue still present in current HEAD; fix is narrow (1-30 line change), test-preserving, no contract change. This is the default for docstrings, comments, dead-code removal, magic-number extraction, log-added-not-silent, unused-imports, unreachable-code-deletion, sentinel-rename with no consumer change.

       **`already-resolved`** — grep or source read confirms the issue no longer holds. Example: P3-NI-03 claims `OPENROUTER_BASE_URL` is defined but bypassed; Phase 8 CLEAN-04 already centralized this. If the grep check passes (`grep -n '"https://openrouter.ai/api/v1"' tools/analysis/openrouter_video_analyzer.py` returns exactly one line — the constant definition), disposition = `already-resolved`, reference the Phase 8 commit SHA that fixed it.

       **`defer`** — fix would require (a) new behavior, (b) contract/schema change, (c) new feature, or (d) tests that don't exist yet and would require research. Example: P5-NR-04 (round-trip validation after _write_staging) would add a disk-read + schema-validate to every synthesis — that's a behavior change, defer. Example: P3-LO-04 (markdown fence stripping in prompt-embedded fallback) — needs a new unit test with a fake claude response; borderline, may defer. Per 11-CONTEXT.md, deferred items go to `.planning/REQUIREMENTS.md` Future Requirements with ID `NITS-DEF-<P3|P4|P5>-<LO|NI|LR|NR>-<NN>` and one-line rationale.

    3. Write `11-02-SUMMARY.md` with this structure (placeholder SHAs — filled in Task 2):

        ```markdown
        # Phase 11-02: NITS-01 Absorption — Classification

        **Catalogue source:** 03-REVIEW.md (9 items) + 04-REVIEW.md (11 items) + 05-REVIEW.md (10 items) = 30 items total
        **Classified:** <date>
        **Target test baseline:** >=687 passing

        ## Classification Table

        | ID | Origin | File | Disposition | Commit / Evidence | Rationale |
        |----|--------|------|-------------|-------------------|-----------|
        | P3-LO-01 | 03-REVIEW.md#LO-01 | tools/analysis/openrouter_video_analyzer.py:68 | fix | TBD | Delete dead FALLBACK_MODEL constant + test import |
        | P3-LO-02 | 03-REVIEW.md#LO-02 | tools/analysis/openrouter_video_analyzer.py:455 | fix | TBD | Add one-line comment explaining asymmetric ladder |
        | P3-LO-03 | 03-REVIEW.md#LO-03 | tools/analysis/openrouter_video_analyzer.py:355-359 | <fix|already-resolved|defer> | <TBD|commit-SHA|NITS-DEF-P3-LO-03> | <…> |
        | ... | ... | ... | ... | ... | ... |
        | P5-NR-04 | 05-REVIEW.md#NR-04 | lib/pipeline_synthesizer.py:519-540 | defer | NITS-DEF-P5-NR-04 | Behavior change — adds disk-read + schema-validate on every synthesis |

        ## Summary Counts

        - `fix`: <N> items (each = one atomic commit in Task 2)
        - `already-resolved`: <M> items (documented with grep evidence or prior commit SHA)
        - `defer`: <K> items (added to REQUIREMENTS.md Future Requirements)
        - Total: 30 (matches catalogue — or record discrepancy if count is 28 or 32)

        ## Deferred Items (for REQUIREMENTS.md Future Requirements)

        ### NITS-DEF-P5-NR-04
        <rationale>
        ```

    4. Commit the classification document before starting Task 2 so the intent is locked:
       `docs(11-02): classify 30 LO/NI findings (<N> fix / <M> already-resolved / <K> defer)`

    5. If the classification reveals more `already-resolved` than expected, that's a sign Phase 8-10 did more sweep than planned — note in SUMMARY but don't manufacture fixes. If nearly all items are `defer`, that's a sign the catalogue was over-scoped and that's also fine — report it honestly.

    Do NOT edit any source file in this task. Task 1 is purely reading + classification + SUMMARY write + commit.
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-drift-hygiene/11-02-SUMMARY.md` exists
    - Contains a table with exactly one row per LO/NI/LR/NR item (count matches the three REVIEW.md files — typically 30, record actual)
    - Every row has: ID (with P3/P4/P5 prefix), Origin link, File path, Disposition (fix|already-resolved|defer), Commit/Evidence placeholder, Rationale (>=5 words)
    - Disposition counts summed; total matches catalogue count
    - Deferred items listed separately with rationale sentences
    - Exactly one commit lands for this task: `docs(11-02): classify <N> LO/NI findings (...)`
    - Running `grep -c "^| P[345]-" .planning/phases/11-drift-hygiene/11-02-SUMMARY.md` returns the total item count
    - `pytest tests/ --ignore=tests/qa -q` reports same pass count as before Task 1 (no source changes yet)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/11-drift-hygiene/11-02-SUMMARY.md &amp;&amp; count=$(grep -c "^| P[345]-" .planning/phases/11-drift-hygiene/11-02-SUMMARY.md) &amp;&amp; echo "classified items: $count" &amp;&amp; test "$count" -ge 28 -a "$count" -le 32 &amp;&amp; pytest tests/ --ignore=tests/qa -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    - Classification document committed
    - Every LO/NI item has an explicit disposition
    - Deferred-items rationale sentences are complete
    - Test suite unchanged from baseline (no source edits in this task)
  </done>
</task>

<task type="auto">
  <name>Task 2: FIX PASS — execute the classification as atomic commits</name>
  <files>
    <!-- Files touched depends on Task 1's classification. Actual list determined by classified 'fix' rows. -->
    <!-- Probable set (matching files_modified frontmatter): -->
    tools/analysis/openrouter_video_analyzer.py, tests/unit/test_openrouter_video_analyzer.py,
    lib/analysis_merger.py, lib/chunked_analyzer.py, lib/video_chunker.py,
    lib/pipeline_synthesizer.py, lib/llm_fill.py,
    tests/contracts/test_phase5_synthesis.py, tests/unit/test_pipeline_synthesizer.py,
    .planning/REQUIREMENTS.md,
    .planning/phases/11-drift-hygiene/11-02-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/11-drift-hygiene/11-02-SUMMARY.md` (the classification table produced by Task 1 — this IS the fix queue)
    - For each `fix` item: re-open its referenced file+line before editing to confirm the issue still holds (git status may have evolved between Task 1 and Task 2; low probability but cheap check)
    - For each `already-resolved` item: re-run the grep or source-check that proved resolution, record output in SUMMARY evidence column
    - `.planning/phases/11-drift-hygiene/11-CONTEXT.md` (D-02 commit rules: one commit per fix, no same-file coalescing unless literally one-token rename)
  </read_first>
  <action>
    Execute the classification from Task 1. **This task is structured as a LOOP — one iteration per 'fix' row.** Each iteration = 1 atomic commit.

    **Per-fix iteration protocol:**

    For each row with disposition `fix`, in any order (topologically preferred: same-file fixes grouped chronologically to minimize merge friction, but still one commit each):

    1. Re-open the file+line cited in the finding.
    2. Apply the narrowest possible change that resolves the specific finding. Do NOT:
       - Reformat neighboring code ("while I'm here").
       - Fix multiple findings in one commit, even if they touch the same file.
       - Add new features, refactor beyond the finding's scope, or widen the change.
       Do:
       - Run the test that most directly covers the changed code (`pytest tests/... -k <nearest test> -x`) — must pass.
       - Run the targeted file's full test: `pytest tests/unit/<matching_test_file>.py -q` — must pass.
    3. Stage the exact files touched (`git add <path>`) — no `git add -A`.
    4. Commit: `fix(11-nits): <P3-LO-01|...> <short description from SUMMARY rationale, lowercase, no period>`.
       For comment-only or refactor-only changes, use `refactor(11-nits):` or `docs(11-nits):` as the type prefix. Pattern: `(11-nits)` scope is the fingerprint grep can use to find all this plan's commits.
    5. Run `git rev-parse HEAD` and record the SHA in `11-02-SUMMARY.md` row's `Commit / Evidence` cell.
    6. Every 5 commits (cadence — OR at any `fix` that touches the same file as a recent `fix`), run the full suite: `pytest tests/ --ignore=tests/qa -q`. Must stay >=687 passing. If it breaks, revert the last commit (`git revert HEAD --no-edit`) and mark the item as `defer` in SUMMARY with the failure observation.

    **Per-already-resolved item:**

    Fill in the `Commit / Evidence` column with the grep output or prior commit SHA that proves resolution. No new commit.

    Example evidence:
    ```
    P3-NI-03 — `grep -n '"https://openrouter.ai/api/v1"' tools/analysis/openrouter_video_analyzer.py`
    -> line 66 (OPENROUTER_BASE_URL definition only); Phase 8 CLEAN-04 commit <SHA> removed call-site literal
    ```

    **Per-deferred item:**

    1. Append to `.planning/REQUIREMENTS.md` under `## Future Requirements` -> `### Code Hygiene (deferred from NITS-01)`:
       ```markdown
       - **NITS-DEF-P<N>-<LO|NI|LR|NR>-<NN>**: <one-line issue> (deferred from Phase 11-02; see `.planning/milestones/v2.0-phases/<phase>-*/0<N>-REVIEW.md#<id>` for full context). Rationale: <why deferred>.
       ```
    2. SUMMARY row's `Commit / Evidence` cell = the NITS-DEF-* ID.

    **After all iterations:**

    1. Finalize `11-02-SUMMARY.md`:
       - Replace all `TBD` commit cells with actual SHAs.
       - Compute final counts (fixed / already-resolved / deferred).
       - Add "Commit count" line: `git log --oneline --grep='(11-nits)' | wc -l` — must equal fix count.
       - Add "Full suite result" line from final `pytest tests/ --ignore=tests/qa -q` run.
    2. If `.planning/REQUIREMENTS.md` was edited, commit it: `docs(11-02): defer <K> LO/NI items to Future Requirements (NITS-01)`.
    3. Commit the final SUMMARY: `docs(11-02): finalize classification with commit SHAs and evidence`.

    **Full suite sanity at plan end:**

    ```bash
    pytest tests/ --ignore=tests/qa -q 2>&1 | tail -5
    ```

    Result line must show `>= 687 passed`. Anything less = at least one fix regressed; bisect with `git bisect` against the fix commits and revert the offender.

    **Commit count verification:**

    ```bash
    fix_count=$(grep -c "^| P[345]-.* fix " .planning/phases/11-drift-hygiene/11-02-SUMMARY.md)
    commit_count=$(git log --oneline --grep='(11-nits)' | wc -l)
    test "$commit_count" -ge "$fix_count"
    ```

    The `>=` (not `==`) accommodates the REQUIREMENTS.md defer-commit and the SUMMARY-finalize commit, both of which also have `(11-02)` scope. Count should still be within a reasonable band — report exact numbers in the PR-ready summary at end.
  </action>
  <acceptance_criteria>
    - Every row in `11-02-SUMMARY.md` has a non-TBD `Commit / Evidence` cell
    - For `fix` rows: cell is a 7+ char hex SHA
    - For `already-resolved` rows: cell cites a grep output or prior commit SHA
    - For `defer` rows: cell cites a `NITS-DEF-*` ID that resolves to a line in `.planning/REQUIREMENTS.md`
    - `git log --oneline --grep='(11-nits)' | wc -l` >= fix count (one commit per fix minimum)
    - `git log --oneline --grep='(11-nits)'` shows NO monolithic "absorb all nits" commit — each commit's message references exactly ONE P3-*, P4-*, or P5-* ID
    - `pytest tests/ --ignore=tests/qa -q` reports >=687 passing after final commit
    - `.planning/REQUIREMENTS.md` Future Requirements section contains one entry per `defer` item with `NITS-DEF-*` ID
    - 11-02-SUMMARY.md final Commit count / Full suite result lines are populated
    - No behavior change: no tests renamed, no test deletions (only additions allowed if a finding mandated one — e.g. P4-LO-06 zero-duration guard requires a regression test)
  </acceptance_criteria>
  <verify>
    <automated>fix_count=$(grep -c "^| P[345]-.* fix " .planning/phases/11-drift-hygiene/11-02-SUMMARY.md); commit_count=$(git log --oneline --grep='(11-nits)' | wc -l); echo "fix_count=$fix_count commit_count=$commit_count"; test "$commit_count" -ge "$fix_count" &amp;&amp; pytest tests/ --ignore=tests/qa -q 2>&amp;1 | tail -3 | tee /tmp/11-02-final.txt &amp;&amp; grep -E "[0-9]+ passed" /tmp/11-02-final.txt | awk '{for(i=1;i&lt;=NF;i++) if($i=="passed,") print $(i-1)}' | head -1 | awk '{if ($1+0 &gt;= 687) exit 0; else exit 1}'</automated>
  </verify>
  <done>
    - All 30 (±2) items have a final disposition with evidence
    - Each `fix` landed as its own atomic commit with `(11-nits)` scope
    - Each `defer` has a `NITS-DEF-*` entry in REQUIREMENTS.md
    - Full suite stays >=687 passing
    - Final `docs(11-02): finalize classification ...` commit lands with SHAs filled in
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LO/NIT-level source edits | Internal code hygiene; no trust boundary crossed by any individual fix |
| REQUIREMENTS.md Future Requirements append | Repo-tracked metadata, no runtime effect |
| Full test suite | Regression gate — any fix that moves pass count down is treated as a failure and reverted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | source files (many) | mitigate | Per-fix atomic commits enable git-bisect of any regression; each commit is individually revertible; classification document is committed before fixes so intent is locked |
| T-11-02-02 | Denial of Service | full test suite | mitigate | Cadence rule (full suite every 5 commits, or on same-file fixes); acceptance gate at plan end requires >=687 passing; any regression triggers revert + reclassification to `defer` |
| T-11-02-03 | Information Disclosure | openrouter_video_analyzer.py error paths (P3-LO-03 if fixed) | mitigate | P3-LO-03's fix (if applied) ADDs key-redaction to exception strings — defense-in-depth; existing T-03-06 test (API-key-in-error) continues to gate |
| T-11-02-04 | Repudiation | which item got fixed vs. deferred | mitigate | 11-02-SUMMARY.md classification table IS the audit trail; each row cites commit SHA or NITS-DEF-* ID; git log with `--grep='(11-nits)'` is the source of truth for fix commits |
</threat_model>

<verification>
- `grep -c "^| P[345]-" .planning/phases/11-drift-hygiene/11-02-SUMMARY.md` >= 28 (catalogue size)
- Every row in SUMMARY has non-TBD disposition + evidence
- `git log --oneline --grep='(11-nits)'` shows N atomic commits (N >= fix count)
- No single commit message matches regex `absorb|batch|all.nits|cleanup.sweep` — these would indicate a monolithic commit anti-pattern
- `pytest tests/ --ignore=tests/qa -q` >= 687 passing at plan end
- `.planning/REQUIREMENTS.md` contains one `NITS-DEF-*` entry per deferred row
- Phase 3 fc-list failures count unchanged (3, pre-existing)
</verification>

<success_criteria>
- NITS-01 closed: every LO/NI item accounted for (fixed | already-resolved | deferred)
- Classification document provides traceable audit trail with commit SHAs and grep evidence
- No silent fixes: each fix has its own commit with a `(11-nits)` scope that grep can find
- No silent deferrals: each defer has an entry in REQUIREMENTS.md Future Requirements
- Test suite preserved: >=687 passing throughout; 3 pre-existing fc-list failures unchanged
- Plan 11-02 commit log reads as: 1 classification doc + N atomic fixes + 1 REQUIREMENTS.md defer-commit + 1 finalize-summary = the whole trail of work is visible and bisectable
</success_criteria>

<output>
After completion, `.planning/phases/11-drift-hygiene/11-02-SUMMARY.md` (already the working doc) contains:
- Final classification table with commit SHAs filled in
- Disposition counts (fix / already-resolved / defer)
- Final test suite result (pass/fail count)
- Commit count verification line
- List of NITS-DEF-* IDs added to REQUIREMENTS.md
- Reference to NITS-01 requirement closure

No separate SUMMARY file needed — 11-02-SUMMARY.md IS the summary, built incrementally through the plan.
</output>
